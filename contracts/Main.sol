// SPDX-License-Identifier: GPL-3.0
pragma solidity ^0.8.0;
pragma experimental ABIEncoderV2;

contract Main{
    
    function encrypt(address userAddress, string memory  role) internal returns (string memory ){
    }

    function decrypt(string memory  token) internal returns (address){
    }

    // comman man => extra fields to fetch
    function getAllTenders() public returns (address[] memory ){
    }

    function getAllOngoingContracts() public returns (address[] memory){
    }
}